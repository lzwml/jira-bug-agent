# Security Policy

这个项目会接触 Jira Issue、评论、日志和可能包含敏感信息的附件。部署者应使用
最小权限专用账号，限制项目可见性，并在把内容发送给外部模型前完成企业合规与
脱敏评审。

当前实现：

- 不回写 Jira；
- 不执行或自动解压附件；
- Jira 与模型 Token 只从进程环境读取；
- 限制 Jira 导出根目录、Log 读取根目录、附件大小和 Tool Result 长度；
- 不把远端错误响应体直接暴露给 Agent。

公开仓库建立后，应通过 GitHub Security Advisory 私下报告路径穿越、任意文件
写入、凭据泄露、跨域认证转发或预算绕过。请勿在公开 Issue 粘贴真实企业数据。

